# sap-flex-search-cli
Python CLI to run FlexibleSearch queries against SAP Commerce Cloud 2211 / Hybris via HAC, without the SDK. Handles form login, CSRF tokens, session persistence, and renders results as table/JSON/CSV.
